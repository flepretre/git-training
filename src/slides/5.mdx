# Git
#### Chapitre 5

---

# Le réseau

* remote
* fetch
* push
* pull
* clone

---

## Le réseau

Tout ce qu'on a vu jusqu'à présent est 100% offline

---

## remote

* permet de définir les urls pour des repos distant
* il est possible d'avoir deux repos differents pour le fetch et le push
* il est possible d'avoir plusieurs repos distants

---

## remote

```sh
git remote -v
git remote add flepretre https://github.com/flepretre/git-training
git remote set origin flepretre https://github.com/flepretre/foo
```

---

## fetch

* récupère l'état des repos distants
* fait progresser les branches distantes (origin/toto)
* permet aussi de récupérer les tags
* n'a aucun impact sur les branches locales

---

## fetch

```sh
git fetch
git fetch flepretre
git fetch --tags
git fetch --prune
```

---

## push

* pousse des commits sur un repo distant
* fait progresser la branche distante
* échoue si la progression de la branche n'est pas fast-forward

---

## push

```sh
git push origin master
git push origin master~:master -f
git push origin --tags
git push origin :toto -f
```

---

## pull

* fait un fetch suivit d'un merge
* :warning: par défaut, si le merge est non fast-forward, un commit de merge est créé

```notes
il existe un git pull --rebase mais il vaut mieux savoir faire ça manuellement d'abord
```

---

## TP 5

* créer un compte sur github.com
* créer un repo
* ajouter un remote sur votre repo local
* pusher toutes les branches et tous les tags

---

## TP 5

* supprimer le répertoire de votre repo local
* faire un git clone de votre repo distant

* organisez vous en équipe de 5
* créez votre équipe sur https://classroom.github.com/g/_hi90IgW
* cloner le repo de l'équipe
