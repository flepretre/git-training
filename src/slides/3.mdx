# Git
#### Chapter 3

---

# Les références

* log
* HEAD
* branches
* tags
* stash

```notes
- git fonctionne par référence pour
-- le working directory
-- les branches
```

---

## log
* pour observer le graph

```sh
git log
git log --oneline --decorate --graph master foo bar
git log --oneline --decorate --graph --all
```

---

## HEAD

* HEAD référence la branche sur lequel votre working directory est basé

```sh
git checkout foo
```

---

## branches

* pointe sur un commit
* la branche courante est celle sur laquelle pointe HEAD
* lors d'un commit la branche courante est déplacée sur le sha1 du nouveau commit

---

## branches

```sh
git branch foo
git checkout foo
git branch -m bar
```

---

import Branch from '../components/repos/branch';

<Branch />

---

## tags

* comme une branche, mais ne peux pas être déplacé

---

## tags

```sh
git tag v1.0.0
git tag -d v1.0.0
```

---

## stash

* stack (filo)
* versionne ce qui est ajouté à l'index
* et ce qui est modifié mais non ajouté

---

## stash

```sh
git stash save "I name my stash 'cause I'm a clean dev"
git stash list
git stash show @{2}
git stash apply
git stash pop
git stash drop
```

---

import Stash from '../components/repos/stash';

<Stash />

---

## TP 3

* configurer l'alias global "lol" qui exécute la commande
```sh
git log --oneline --decorate --graph
```

---

## TP 3

* créer un nouvelle branche
* positionner vous sur la branche
* ajouter 2-3 lignes au ficher README et commitez les modifications
* observer votre repo avec les commandes vues précédemment

---

## TP 3

* faites encores quelques modifications sur le README
* stashez les modifications
* déplacez vous sur la branche master
* appliquez les modifications

---

## TP 3

* résoudre les conflits si vous en avez et commiter
* créer un tag v1.0.0
